import std.math

fn main() {
    println("Testing imports from std.math")
    const num: i32 = -5;
    if math::abs(num) == 5 {
        print("Correct");
    } else {
        eprint("Incorrect");
        exit(1);
    }
}
